# Hair length options — referenced from hair.txt via __hair/length__
# Ordered short → long. Length-only phrasing (no cut names) so it composes cleanly with style.

cropped
short
shoulder-length
medium length
long
mid-back length
waist-length
hip-length
very long
floor-length
